#+title: Work Plan
#+DATE: <2026-06-10 Wed 13:54>
#+AUTHOR: Mohsen BANAN
#+OPTIONS: toc:4
#+TAGS: me ai aiAndMe

####+BEGIN: b:ai:file/particulars
* AI Working Context
  Working Directory: /bisos/git/bxRepos/bisos-pip/startAiActivity
  File: /bisos/git/auth/bxRepos/bisos-pip/startAiActivity/AI-WorkPlan.org
  Activity: bisos-pip
  Companion Docs: AI-AGENTS.org, AI-WORKFLOW.org, AI-Activity.org, AI-DevStatus.org, README.org

####+END:

This Work Plan file drives the AI collaborative development.
Interactions are broken down into "Stages". Each Stage has a set
of TODOs that I work with Claude to accomplish.

Note to a fresh Claude: read this file together with AI-AGENTS.org, AI-WORKFLOW.org,
AI-Activity.org and AI-DevStatus.org to orient yourself. The =<<Context Confirmation>>=
TODO below is the standard first step, or use =/bx-confirm0= as the automated equivalent.
TODOs tagged =:ai:= are assigned to Claude. After completing a TODO, summarize results
under =----- AI Results: ------= and mark it DONE.

* Stage 1: Confirm AI's Understanding of This Repo's Context

Goal: Confirm AI's understanding of the context and capture that in [[Context Confirmation]] TODO.


* Stage 1 TODOs
** +
** DONE [#A] <<Context Confirmation>> for AI (typically triggered by /bx-confirm0) :ai:
CLOSED: [2026-07-12 Sun 10:50] SCHEDULED: <2026-06-09 Tue>
Based on the series of files starting from CLAUDE.md in this repo,
describe succinctly what you have understood.
Enumerate explicitly which files you have imported and which you have considered.
----- AI Results: ------
Confirmed.
** DONE [#A] <<Repo Structure  Confirmation>> :ai:
CLOSED: [2026-07-12 Sun 10:51] SCHEDULED: <2026-06-09 Tue>
Read this repo in full and briefly confirm that you see it as complete and understand how it works
----- AI Results: ------
Confirmed.
** -

* Stage 2: Fully Implement startAiActivity.cs

Goal: A functioning implementation with starting point files for py-dblock and userConfigFileParams


* Stage 2 TODOs
** +
** DONE [#A] <<Explain>> how you would implement startAiActivity.cs based on exisiting startAiAt.cs :ai:
SCHEDULED: <2026-06-09 Tue>
@./startAiActivity.org
----- AI Results: ------
Four targeted changes to the verbatim copy of =startAiAt.cs=:
1. *=templatesBase= via File Parameter*: remove hardcoded =g_templatesBase=; read from
   =~/.config/bisos/startAiActivity/templatesBase/value= using =bisos.b fp=, with =--templates=
   CLI override. Logic lives in new =userConfigFileParam_csu.py=.
2. *=bystar/= → =mother/=*: rename the baseline directory throughout =initiate.cmnd= and
   the =excludedDirs= set in =examples=.
3. *Pure-Python dblock expansion*: replace =bx-dblock= subprocess with a regex-based
   expander in new =dblock_particulars.py= (replaces content between =####+BEGIN: b:ai:file/particulars=
   and =####+END:= markers). =updateDblock.py= builds the companion-docs list and calls it.
4. *New =setup= command*: writes the =templatesBase= path to the file parameter location,
   creating parent dirs as needed.
No templates are bundled; all install operations remain idempotent.
** DONE [#A] <<Implement>> startAiActivity.cs based on exisiting startAiAt.cs :ai:
SCHEDULED: <2026-06-09 Tue>
Tell me key decisions made while implemeting it.
----- AI Results: ------
Four files created/updated. Verified working with =startAiActivity.cs -i initiate --activity=bisos-pip=.

Key decisions:
- =userConfigFileParam_csu.py= :: Uses =fp.FileParamWriteTo= / =fp.FileParamValueReadFrom= directly
  (not the path-based variants) — =parRoot= is the config dir, =parName= is ="templatesBase"=,
  matching how =bisos.b= stores file params as directories containing a =value= file.
- =dblock_particulars.py= :: Single =re.sub= with =DOTALL= replaces the full block content
  in one pass. Marker is =###\+BEGIN: b:ai:file/particulars= (escaped for regex). Note the
  source uses =####+END:= (4 hashes) but the elisp original uses =###+END:= (3); the regex
  captures whichever is present.
- =updateDblock.py= :: Builds companion-docs list by scanning for a fixed candidate list
  (same files the elisp dblock checks), then delegates to =dblock_particulars.expand=.
- =startAiActivity.cs= :: Added =templates= as an optional param to both =initiate= and
  =commonParamsSpecify=; mandatory only for =setup=. =examples= command now calls
  =templatesBaseGet()= at runtime and handles =None= gracefully instead of crashing.
  =excludedDirs= updated to =mother= (was =bystar=).
** TODO [#A] <<Improve Towards py-dblock>> -- make py imitate :ai:
SCHEDULED: <2026-06-09 Tue>
- updateDblock.expandParticulars(dst, targetDir, activity)  is the specific approach.
- The general approach is to mimic org-mode's model for dblocks.
- create  a py-dblock.cs file. It will have a command named updateDblocks. And takes args that are in files. For each input file it will call updateDblock.expandAll(dst,)
- Each dblock processor, registers a signature with a function in a file like ./py3/bisos/startAiActivity/dblock_particulars.py
- then ./py3/bisos/startAiActivity/updateDblock.py looks for the signatures and applies the function to each.
- Re-read the "Modularization" section of startAiActivity.org to see my intent from the beginning
----- AI Results: ------
** TODO [#A] <<Improve Towards Persistent Parameters>> -- make py imitate :ai:
SCHEDULED: <2026-06-09 Tue>
- Add parPermanence="userConfig" (or an emum) to this. Default is None.
  csParams.parDictAdd(
        parName='templates',
        parDescription="Override templatesBase file parameter for this run.",
        parDataType=None,
        parDefault=None,
        parChoices=[],
        argparseShortOpt=None,
        argparseLongOpt='--templates',
        parPermanence="userConfig"
    )
- Existing setup Cmnd, should become userConfig_set, userConfig_get -- With the example including
  current values.
- The  userConfig_set etc Cmnds will be in a general purpose file which is imported.
- The ~/.config/bisos/csxu/param/value should become generic. So startAiActivity should become startAiActivity.cs
----- AI Results: ------
** -

* Stage 3: Documentation and pypi upload and pipx testing

Goal: Update README for pypi, Update Blee-Panels documentation, upload to pypi and test as pipx


* Stage 3 TODOs
** +
** TODO [#A] Based on this ai context, Write ./README.org
SCHEDULED: <2026-06-09 Tue>
----- AI Results: ------
** TODO [#A] Based on this ai context, Write ./py3/panels/bisos.startAiActivity/_nodeBase_/fullUsagePanel-en.org
SCHEDULED: <2026-06-09 Tue>
** TODO [#A] After upload to pypi, test it as a pipx install
SCHEDULED: <2026-06-09 Tue>
----- AI Results: ------

** -


# Local Variables:
# eval: (setq-local toc-org-max-depth 4)
# eval: (setq-local org-log-done 'time)
# End:
