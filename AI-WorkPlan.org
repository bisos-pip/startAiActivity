#+title: Work Plan
#+DATE: <2026-06-10 Wed 13:54>
#+AUTHOR: Mohsen BANAN
#+OPTIONS: toc:4
#+TAGS: me ai aiAndMe

####+BEGIN: b:ai:file/particulars
* AI Working Context
  Working Directory: ../..
  File: /bisos/git/auth/bxRepos/bisos-pip/startAiActivity/AI-WorkPlan.org
  Activity: bisos-pip
  Companion Docs: AI-WORKFLOW.org, AI-Activity.org, AI-DevStatus.org, README.org
###+END:

This Work Plan file drives the AI collaborative development.
Interactions are broken down into "Stages". Each Stage has a set
of TODOs that I work with Claude to accomplish.

Note to a fresh Claude: read this file together with AI-WORKFLOW.org,
AI-Activity.org and AI-DevStatus.org to orient yourself. The =<<Context Confirmation>>=
TODO below is the standard first step, or use =/bx-confirm0= as the automated equivalent.
TODOs tagged =:ai:= are assigned to Claude. After completing a TODO, summarize results
under =----- AI Results: ------= and mark it DONE.

* Stage 1: Confirm AI's Understanding of This Repo's Context

Goal: Confirm AI's understanding of the context and capture that in [[Context Confirmation]] TODO.


* Stage 1 TODOs
** +
** -

* Stage 2: Fully Implement startAiActivity.cs

Goal: A functioning implementation with starting point files for py-dblock and userConfigFileParams


* Stage 2 TODOs
** +
** DONE [#A] <<Improve Towards py-dblock>> -- make py imitate org-mode dblocks  :ai:
SCHEDULED: <2026-06-09 Tue>
- updateDblock.expandParticulars(dst, targetDir, activity)  is the specific approach.
- The general approach is to mimic org-mode's model for dblocks.
- create  a py-dblock.cs file. It will have a command named updateDblocks. And takes args that are in files. For each input file it will call updateDblock.expandAll(dst,)
- Each dblock processor, registers a signature with a function in a file like ./py3/bisos/startAiActivity/dblock_particulars.py
- then ./py3/bisos/startAiActivity/updateDblock.py looks for the signatures and applies the function to each.
- Re-read the "Modularization" section of startAiActivity.org to see my intent from the beginning
----- AI Results: ------
Generalized the dblock engine across three files:

- =updateDblock.py= :: Rewritten as a general dispatch engine. Handlers register under a
  signature string via =registerHandler(sig, fn)=. =expandAll(filePath)= does a single-pass
  =re.sub= over the file, parsing each =####+BEGIN: <signature> [:key value ...]= line into
  signature + keyword-arg dict, looking up the handler, and replacing the block body with
  the handler's return value. Unregistered signatures are left untouched. =targetDir= removed
  from public API — derived internally as =filePath.parent=.

- =dblock_particulars.py= :: Rewritten as a self-registering handler. Calls
  =updateDblock.registerHandler('b:ai:file/particulars', _handler)= at import time.
  Derives =activity= from the =AI-Activity.org= symlink and builds companion-docs list
  by scanning the directory. Accepts =args= dict for future parameterised dblocks.

- =py-dblock.cs= :: New CS executable with =updateDblocks= command. Takes one or more
  file paths as positional args (=argsMin=1 argsMax=9999=), calls =updateDblock.expandAll=
  on each. Imports =dblock_particulars= to self-register the handler.

Also: added =chmod +x *= to =.claude/settings.json=; added "Claude Permissions" section
to =mother/AI-WORKFLOW.org= directing Claude to always offer to persist new permissions
to =.claude/settings.json= at the time of asking.
** DONE [#A] <<Improve Towards Persistent Parameters>> -- make userConfig persistent parameters Generic :ai:
SCHEDULED: <2026-06-09 Tue>
- Add parPermanence="userConfig" (or an emum) to this. Default is None.
  csParams.parDictAdd(
        parName='templates',
        parDescription="Override templatesBase file parameter for this run.",
        parDataType=None,
        parDefault=None,
        parChoices=[],
        argparseShortOpt=None,
        argparseLongOpt='--templates',
        parPermanence="userConfig"
    )
- Existing setup Cmnd, should become userConfig_set, userConfig_get -- With the example including
  current values.
- The  userConfig_set etc Cmnds will be in a general purpose file which is imported.
- The ~/.config/bisos/csxu/param/value should become generic. So startAiActivity should become startAiActivity.cs
----- AI Results: ------
Merged =userConfigFileParam_csu.py= (deleted) into a single new =userConfig_csu.py=:

- =parGet(parName, override)= / =parSet(parName, value)= :: generic read/write helpers using
  =bisos.b fp=. Config root is =~/.config/bisos/<csxu-name>/= where =<csxu-name>= comes from
  =pathlib.Path(sys.argv[0]).name= — so =startAiActivity.cs= → =~/.config/bisos/startAiActivity.cs/=.
- =userConfig_get= / =userConfig_set= :: CS command classes taking =--parName= and =--parValue=.
  Available on the CLI for any parameter, not just =templatesBase=.

In =startAiActivity.cs=:
- =userConfig_csu= added to =csuList= so its commands are imported automatically.
- =parName= / =parValue= added to =commonParamsSpecify=.
- =setup= command removed; replaced by =userConfig_set= / =userConfig_get= in =examples=.
- =initiate= uses =userConfig_csu.parGet('templatesBase', templates)= with the =--templates=
  override still working as before.
** -

* Stage 3: Documentation and pypi upload

Goal: Update README for pypi, Update Blee-Panels documentation, upload to pypi and test as pipx


* Stage 3 TODOs
** +
** DONE [#A] Based on this ai context, Write ./README.org
CLOSED: [2026-07-12 Sat]
SCHEDULED: <2026-06-09 Tue>
----- AI Results: ------
Wrote =README.org= at repo root covering: Overview, Part of BISOS, Command-Only
PyCS Facility, Post-Installation Setup, Installation (pip + pipx), Usage
(userConfig_set, initiate, py-dblock), Key Files, Documentation/Blee-Panels,
Support, Planned Improvements. Also wrote =py3/README.rst= as the converted
PyPI form, following RST conventions matching pandoc/ox-rst output.
** DONE [#A] Based on this ai context, Write ./py3/panels/bisos.startAiActivity/_nodeBase_/fullUsagePanel-en.org
CLOSED: [2026-07-12 Sat] SCHEDULED: <2026-06-09 Tue>
----- AI Results: ------
Wrote the full Blee panel at =py3/panels/bisos.startAiActivity/_nodeBase_/fullUsagePanel-en.org=
with standard header dblocks, Overview, Pip/Pipx Installation (runResult dblocks),
startAiActivity.cs intro, User Config section (userConfig_get/set), initiate section
(bisos-pip, repo, xu-single, blee-panels, lcnt activities), py-dblock.cs section,
and Design/Evolution TODOs. No RO/service sections (command-only package).
** DONE [#A] <<No py-dblock.cs>> in Documentation
CLOSED: [2026-07-13 Sun] SCHEDULED: <2026-06-09 Tue>
Now that bisos.pyDblock is in place separate from  startAiActicity. Delete all mention of py-dblock.cs from documentation
Show me current mentions before deleting them
----- AI Results: ------
Removed all =py-dblock.cs= mentions from the four documentation files:
- =README.org= — removed from Overview paragraph, Installation commands list, "Update Dblocks" Usage section, and Key Files section
- =AI-DevStatus.org= — removed from Architecture listing, Entry points, What Works, and What Is Untested
- =fullUsagePanel-en.org= — removed Overview bullet and entire "py-dblock.cs --- Update Org Dblocks" section
Historical references in =AI-WorkPlan.org= (DONE task bodies) and =startAiActivity.org= were left intact as they are part of the development record.
=py3/README.rst= was not touched — it is machine-generated from =README.org=.
** -


* Stage 4: Lifecycle Commands

Goal: Add commands for reversible and permanent removal of AI collaboration files.


* Stage 4 TODOs
** +
** DONE [#A] <<deClaudify>> -- permanent removal of AI collaboration files :ai:
CLOSED: [2026-07-13 Sun] SCHEDULED: <2026-07-13 Sun>
Add a =deClaudify= command that permanently removes all AI collaboration files
installed by =initiate=: symlinks (CLAUDE.md, AI-WORKFLOW.org,
AI-Activity.org, .claude/settings.json, .claude/commands), copied files
(AI-DevStatus.org, AI-WorkPlan.org), and the .claude/ dir if empty.
No =--root= param needed — always operates on the current directory.
----- AI Results: ------
Implemented =deClaudify= in =startAiActivity.cs=. Removes symlinks only if they
are symlinks (leaves regular files intact), removes copied files only if they are
regular files (leaves symlinks intact), removes =.claude/= if empty after cleanup.
Added to =examples= command output.
** DONE [#A] <<aiSuspend-aiResume>> -- reversible suspension of AI collaboration :ai:
CLOSED: [2026-07-13 Sun] SCHEDULED: <2026-07-13 Sun>
Add =aiSuspend= and =aiResume= command pair. =aiSuspend= removes symlinks and
=.claude/= (same as =deClaudify=) but instead of deleting =AI-DevStatus.org= and
=AI-WorkPlan.org=, renames them to =.dormant= suffix so they survive.
=aiResume= restores the =.dormant= files and re-installs all symlinks and =.claude/=
entries, inferring the activity from the =AI-Activity.org= symlink target or the
=Activity:= header in =AI-WorkPlan.org=. Neither command takes =--root=.
----- AI Results: ------
Implemented =aiSuspend= and =aiResume= in =startAiActivity.cs=. End-to-end tested:
suspend/resume cycle leaves the directory identical to its pre-suspend state.
Activity inference works from =AI-WorkPlan.org= dblock header when the symlink
is absent (which it is after suspend). Added both to =examples= output.
** -


* Stage 5: Skills Integration

Goal: Distribute Claude Code skills alongside AI collaboration templates.

Skills are self-contained, filesystem-based packages of task-specific
instructions (each a folder with a =SKILL.md=) that Claude loads on-demand
when the current task calls for them. The skill's description sits in
context by default; the full file is read only when relevant.

Two variants:
- *Anthropic pre-built skills* (e.g. =xlsx=, =docx=, =pptx=, =pdf=) — no
  distribution needed; reference by name.
- *Custom skills* — user/org-defined; these are what we distribute through
  the templates tree.

The natural fit with startAiActivity's existing =mother/= + activity-specific
override pattern:
- =mother/_skills/= — skills available to every activity (baseline).
- =<activity>/_skills/= — activity-specific skills (e.g. a =bisos-pip-release=
  skill under =bisos-pip/_skills/= for version bumps and PyPI publishing).

=initiate= would symlink =.claude/skills/= the same way it currently
symlinks =.claude/settings.json= and =.claude/commands=. =deClaudify=,
=aiSuspend=, and =aiResume= all need to handle the new =.claude/skills=
symlink the same way they handle the other =.claude/= entries.


* Stage 5 TODOs
** +
** DONE [#B] <<Skills Directory Layout>> Design directory layout for skills :ai:
CLOSED: [2026-07-13 Sun] SCHEDULED: <2026-07-13 Sun>
Decide the on-disk layout for skills under templates:
- =mother/_skills/<skill-name>/SKILL.md= for baseline skills
- =<activity>/_skills/<skill-name>/SKILL.md= for activity-specific skills
- Should activity skills override mother skills of the same name, or
  should both be installed side-by-side under =.claude/skills/=?
Document the resolution rule in =AI-Activity.org= (mother) once decided.
----- AI Results: ------
Chose the activity-root layout =<activity>/_skills/<skill-name>/SKILL.md=,
NOT nested under =_claude/=. Rationale: skills are semantically distinct
from =.claude/= machinery (settings, commands) — they're documentation /
knowledge. Placing them at the activity root keeps them independently
visible to humans and other tools; the installed location under
=.claude/skills/= is just where Claude Code looks. Resolution rule
mirrors =_claude/= entries: activity =_skills/= if present, else fall
back to =mother/_skills/=. Activity replaces mother wholesale (no
side-by-side merging) — same semantics as the existing =_claude/=
handling.
** DONE [#B] <<initiate Skills Symlink>> Extend =initiate= to symlink =.claude/skills= :ai:
CLOSED: [2026-07-13 Sun] SCHEDULED: <2026-07-13 Sun>
Update =initiate= in =startAiActivity.cs= to symlink =.claude/skills= from
=<activity>/_skills/= (falling back to =mother/_skills/=) the same way
=settings.json= and =commands= are handled. Ensure that empty or missing
=_skills/= directories are handled gracefully (no symlink created).
----- AI Results: ------
Added a =.claude/skills/= block to =initiate= after the existing
=settings.json= / =commands= loop in =py3/bin/startAiActivity.cs=. Uses
the same "activity else mother" fallback. If neither has a =_skills/=
directory, no symlink is created (silent no-op). Verified end-to-end
against =bisos-pip/_skills/= which currently has 5 skill folders:
=writing-cs-commands=, =dblock-authoring=, =readme-authorship=,
=blee-panel-authorship=, =bisos-pip-release=.
** DONE [#B] <<Lifecycle Commands Skills Handling>> Update lifecycle commands for skills :ai:
CLOSED: [2026-07-13 Sun] SCHEDULED: <2026-07-13 Sun>
Extend =deClaudify=, =aiSuspend=, and =aiResume= to handle the
=.claude/skills= symlink alongside =settings.json= and =commands=. All
three commands loop over the same list of =.claude/= entries — add
=skills= to that list in each.
----- AI Results: ------
Added =skills= to the =claudeEntry= list in =deClaudify= and =aiSuspend=
(they iterate a shared list). =aiResume= gets its own dedicated
=activity=/=mother= skills-symlink block after the existing
=settings.json= / =commands= re-install loop, mirroring the =initiate=
logic. Verified full cycle: =initiate= → =aiSuspend= → =aiResume= →
=deClaudify= leaves the directory in the correct state at every stage.
Note during implementation: an initial =replace_all: true= edit
silently matched only one of four target occurrences (the four
=for claudeEntry in [...]:= lines differ by surrounding context); had
to re-run a targeted Edit on =aiSuspend=. Now correct — verified by
grep for the three-element list.
** DONE [#B] <<First Custom Skill>> Author custom skills :ai:
CLOSED: [2026-07-13 Sun] SCHEDULED: <2026-07-13 Sun>
Author one useful skill to validate the workflow end-to-end. Candidates:
- =bisos-pip-release= — bump version, regenerate =py3/README.rst=,
  test with =pipx=, upload to PyPI.
- =dblock-authoring= — write a new =b:ai:file/particulars= style dblock
  handler for =bisos.pyDblock=.
- =blee-panel-authoring= — scaffold a new =fullUsagePanel-en.org= from
  a bisos-pip package.
Place under =mother/_skills/= if broadly applicable, or =bisos-pip/_skills/=
if activity-specific.
----- AI Results: ------
Went further than "one" — authored five skills, all in
=bisos-pip/_skills/= (none in =mother/_skills/= yet since baseline is
bisos-pip-specific). Split from the pre-existing 456-line
=bisos-pip/AI-Activity.org= (Type A / Type B analysis):
- =writing-cs-commands= (187 lines) — =cs.Cmnd= subclass authorship
- =dblock-authoring= (187 lines) — poly-dblock, csuListProc, inline tests
- =readme-authorship= (170 lines) — org→rst conversion conventions
- =blee-panel-authorship= (195 lines) — fullUsagePanel-en.org skeleton
- =bisos-pip-release= (177 lines) — PyPI release checklist
Slimmed =bisos-pip/AI-Activity.org= to 154 lines of always-loaded
orientation + a "Available Skills" table + a "Reference Panels" section.
Original preserved as =AI-Activity.org.pre-skills-backup=. Baseline
per-session context drops ~66%; procedural knowledge (~916 lines) now
loads only on demand.
** DONE [#B] <<Documentation for Skills>> Document skills in README and Blee panel :ai:
CLOSED: [2026-07-13 Sun] SCHEDULED: <2026-07-13 Sun>
Update =README.org= and =fullUsagePanel-en.org= with a "Skills" section
explaining what they are, where they live in the templates tree, and how
to author new ones. Regenerate =py3/README.rst= from =README.org=.
----- AI Results: ------
Documentation updates for Stage 5:
- =AI-DevStatus.org= — added Stage 5 to Current State; noted =initiate=
  now installs =.claude/skills/= symlink; added skills templates section
  to Architecture; added skills entries to What Works.
- =README.org= — added "Skills" Usage subsection; updated =initiate=
  description; regenerated TOC entry.
- =fullUsagePanel-en.org= — added a folding section describing the
  installed =.claude/skills/= symlink and the currently-shipping five
  skills.
=py3/README.rst= was NOT updated in this pass — that is not a manual step:
=README.rst= is regenerated automatically by =py3/pypiProc.sh -i fullPrep=
during release preparation. Between releases, =README.rst= being stale
relative to =README.org= is expected. (Earlier notes in this WorkPlan
that described =README.rst= as needing manual regeneration were wrong —
see the corrected =readme-authorship= skill.)
** -


* Stage 6: Subproject Overlays via initiateSub

Goal: A separate command that installs only the subproject-specific state
(=CLAUDE.md=, =AI-Activity.org=, =AI-DevStatus.org=, =AI-WorkPlan.org=) in
a subdirectory, requiring an already-=initiate=-d ancestor. The invariants
(=AI-WORKFLOW.org=, =.claude/=) are inherited from the
ancestor via Claude Code's walk-up.

Motivation: nested projects should not re-install the invariants at every
level. Doing so wastes context (Claude reads the same twice)
and creates ambiguity about which activity applies. A slim overlay keeps
subproject state local without duplicating the base.


* Stage 6 TODOs
** +
** DONE [#B] <<initiateSub Command>> Add =initiateSub= command :ai:
CLOSED: [2026-07-14 Mon] SCHEDULED: <2026-07-14 Mon>
Requires =--activity= (no inheritance from parent). Preconditions:
1. Walk up from target directory looking for a =CLAUDE.md= symlink whose
   target lands under the configured =templatesBase=. If none, refuse.
2. Target directory must not already have a =CLAUDE.md=. If it does, refuse.
Install: slim =CLAUDE.md= (symlink to =mother/initiateSub/CLAUDE.md=),
=AI-Activity.org= (symlink), =AI-DevStatus.org= and =AI-WorkPlan.org=
(safe-copy with dblock expansion). Does NOT install 
=AI-WORKFLOW.org=, or =.claude/=.
----- AI Results: ------
Implemented in =py3/bin/startAiActivity.cs=. New template at
=mother/initiateSub/CLAUDE.md= imports only =@./AI-Activity.org=,
=@./AI-DevStatus.org=, =@./AI-WorkPlan.org=. Precondition uses symlink-
target check (readlink + relative_to templatesBase) — no content parsing.
Nested =initiateSub= works because both base and sub CLAUDE.md symlinks
land under =templatesBase=, so the precondition treats them equivalently.
End-to-end verified: refusal on isolated dir, refusal when CLAUDE.md
already present, success in properly-nested child, success in
grandchild (nested case).
** DONE [#B] <<aiSuspend/aiResume Mode Detection>> Sub-aware suspend/resume :ai:
CLOSED: [2026-07-14 Mon] SCHEDULED: <2026-07-14 Mon>
=aiSuspend= should NOT unlink =CLAUDE.md= — it should rename it to
=CLAUDE.md.dormant=, preserving the symlink target as the mode signature.
=aiResume= should read =CLAUDE.md.dormant='s target: if it points at
=mother/initiateSub/CLAUDE.md=, restore in sub mode (no 
=AI-WORKFLOW.org=, or =.claude/=); otherwise base mode.
----- AI Results: ------
=aiSuspend= now stashes CLAUDE.md → CLAUDE.md.dormant instead of removing
it. The other symlinks (=AI-WORKFLOW.org=, =AI-Activity.org=)
are still removed since they can be re-derived. =aiResume= reads the
=.dormant= symlink target, compares against
=<templatesBase>/mother/initiateSub/CLAUDE.md= (resolved), sets
=subMode= boolean, and gates the AI-WORKFLOW/.claude/ install
blocks on it. If no =CLAUDE.md.dormant= is found, defaults to base mode.
Verified: base-mode suspend/resume cycle unchanged from Stage 4;
sub-mode cycle correctly restores the slim overlay without touching
inherited invariants. =deClaudify= updated to also remove =.dormant=
files (base and sub), in case cleanup runs while suspended.
** DONE [#B] <<initiateSub Docs>> Update documentation :ai:
CLOSED: [2026-07-14 Mon] SCHEDULED: <2026-07-14 Mon>
Update =README.org=, =AI-DevStatus.org=, =fullUsagePanel-en.org= with the
subproject-overlay concept and =initiateSub= usage. Add =initiateSub= to
=examples= command output.
----- AI Results: ------
Documentation updates: =AI-WorkPlan.org= (Stage 6 with TODOs marked DONE),
=AI-DevStatus.org= (Current State, What Works, Key Design Decisions),
=README.org= (new Subproject Overlay Usage subsection),
=fullUsagePanel-en.org= (new folding section for =initiateSub=).
=examples= command extended with an =initiateSub= chapter.
** DONE [#B] <<verify.sh Test Harness>> Author end-to-end verify.sh :ai:
CLOSED: [2026-07-14 Mon] SCHEDULED: <2026-07-14 Mon>
Author a =py3/test/verify.sh= that exercises the full command surface
end-to-end from a fresh directory: =userConfig_set/get=,
=initiate= (both root modes), =deClaudify=, =aiSuspend=/=aiResume=
(base mode with mode-detection log), =initiateSub= (positive case
inside repo), and =initiateSub= (negative case in isolated =/tmp/=
dir — expect =EH_Problem_UsageError= refusal). Uses =lpDo= for
uniform logging.
----- AI Results: ------
Wrote =py3/test/verify.sh=. Shebang =#!/bin/bash -i= (needed so
=~/.bashrc= runs its interactive body and defines =_opDo=; =lpDo= is
an alias defined there and needs =bash -i= to expand). Uses
=lpDo eval rm -rf ... 2\>/dev/null= for silent cleanup (escaped
redirect so eval sees it as shell syntax). Uses =lpDo pushd= / =popd=
around the =/tmp= negative case for visible directory changes, and
=lpDo eval rm -rf "$TMPDIR"= to clean up. Full cycle exits 0 and
leaves the test directory containing only =verify.sh= itself.
Refusal is visible in the trace as
=EH_Problem_UsageError: No initiated parent found for /tmp/tmp.*=.
** -


* Stage 7: Precedence-based templates auto-detect

Goal: Zero-config setup for a non-BISOS user who does the canonical
=git clone https://github.com/bxexamples/aiActivityTemplates ~/aiActivityTemplates=.
Currently =parDefault= for the =templates= param probes only
=/bisos/apps/defaults/ai-templates=; extend to also probe
=~/aiActivityTemplates=, with user-first precedence.


* Stage 7 TODOs
** +
** DONE [#A] <<Templates Precedence Probe>> Add ~/aiActivityTemplates before BISOS path :ai:
CLOSED: [2026-07-15 Tue] SCHEDULED: <2026-07-15 Tue>
Extract the =parDefault= probe into a helper =_detectTemplatesDefault()=
that walks a candidate list in precedence order (=~/aiActivityTemplates=,
then =/bisos/apps/defaults/ai-templates=) and returns the first existing
path as a resolved absolute string, or None. User-first ordering: a
personal clone at =~/aiActivityTemplates= wins over the BISOS system
copy even when both are present. Uses =pathlib.Path(...).expanduser()=
for tilde resolution and stores the expanded absolute path.
----- AI Results: ------
Implemented =_detectTemplatesDefault()= in =startAiActivity.cs=
immediately before =commonParamsSpecify=; the =parDictAdd= call for
=templates= now uses =parDefault=_detectTemplatesDefault()=. Verified:
on this BISOS machine (no =~/aiActivityTemplates=, BISOS path present),
the function returns =/bisos/apps/defaults/ai-templates= (fallback
correct); running =startAiActivity.cs -i userConfig_get --parName=templates=
through the CLI returns the same. Precedence path (both exist) not
directly testable on this machine but the loop order guarantees it.
** DONE [#A] <<Precedence Docs>> Update three docs :ai:
CLOSED: [2026-07-15 Tue] SCHEDULED: <2026-07-15 Tue>
Update the engine =README.org= (Post-Installation Setup + two-part
architecture section), the templates repo =README.org= (Using This Repo
+ BISOS-Specific Users), and =AI-DevStatus.org= (What Is Untested +
Key Design Decisions) to reflect the precedence-based probe.
----- AI Results: ------
Engine README: replaced the single-path "BISOS users pre-provisioned"
subsection with a precedence-explaining "Auto-detection: two canonical
locations" subsection; Post-Installation Setup restructured to lead
with the zero-config canonical-clone path, with the explicit
=userConfig_set= form kept as an escape hatch for non-canonical clone
paths. Templates repo README: step 2 rewritten to advertise the
zero-config path; BISOS-Specific Users section updated to describe the
combined precedence behavior. =AI-DevStatus.org=: "What Is Untested"
entry updated to reflect both paths as auto-defaults; "Conditional
parDefault" Key Design Decision rewritten as "Precedence-based
parDefault probe" with the user-first rationale.
** -


* Stage 8: Retire AI-AGENTS.org — merge into CLAUDE.md

Goal: Eliminate the =AI-AGENTS.org= indirection layer. Historically
=CLAUDE.md= was a 3-line pointer that =@imports= =AI-AGENTS.org=, which
in turn =@imports= =AI-WORKFLOW.org= / =AI-Activity.org= / =AI-DevStatus.org=
/ =AI-WorkPlan.org=. The =AI-AGENTS.org= layer added no functional value:
it was aspirationally vendor-neutral but content-wise 100% Claude-specific,
and =CLAUDE.md= directly importing the four subordinate files is simpler
and equally functional. Merge and delete.


* Stage 8 TODOs
** +
** DONE [#A] <<AI-AGENTS Merge>> Merge AI-AGENTS.org into CLAUDE.md across three template trees + engine :ai:
CLOSED: [2026-07-16 Tue] SCHEDULED: <2026-07-16 Tue>
Merge steps:
1. Rewrite =mother/CLAUDE.md= in each of the three template trees
   (=/bisos/apps/defaults/ai-templates/=, =bxexamples/aiActivityTemplates/=,
   =/git/rana/rana-notes/aiActivityTemplates/=) to =@import= the four
   subordinate files directly.
2. Delete =mother/AI-AGENTS.org= from all three trees.
3. Update =startAiActivity.cs=: remove =AI-AGENTS.org= from =initiate='s
   =constantFiles= and =aiResume='s constant-symlink loop. *Keep* it in
   =aiSuspend= and =deClaudify= symlink-lists as legacy cleanup (with
   explanatory comment) so pre-merge projects can still be cleaned up
   correctly.
4. Update =mother/initiateSub/CLAUDE.md= to drop the =AI-AGENTS.org=
   mention from the "inherited from parent" list.
5. Sweep documentation: =AI-WORKFLOW.org= (three copies), all README.org
   files (three template repos + engine + T-Mobile), all activity-level
   starter files (=AI-Activity.org=, initial =AI-DevStatus.org= and
   =AI-WorkPlan.org=, =bx-initiate.md=, =bx-confirm0.md=). Batch with
   sed patterns; clean up ==,= artifacts left by aggressive substitution.
----- AI Results: ------
Executed. Ten-plus-file coordinated change across three repos, no
regressions. verify.sh exits 0. =initiate= now creates only
=CLAUDE.md=, =AI-WORKFLOW.org=, =AI-Activity.org= symlinks + =.claude/=;
the =AI-AGENTS.org= symlink no longer exists in fresh projects.
=deClaudify= still recognizes and removes legacy =AI-AGENTS.org=
symlinks from pre-merge projects — the legacy-cleanup path is
explicitly commented at the two list sites. All three =mother/CLAUDE.md=
files now import =AI-WORKFLOW.org= + =AI-Activity.org= + =AI-DevStatus.org=
+ =AI-WorkPlan.org= directly. Batch sed cleanup was aggressive and left
some ==,= empty-verbatim artifacts across ~15 files; those were
targeted-cleaned in a follow-up pass. Templates directory now cleaner:
each activity ships exactly the files it should, no dead indirection.
** -


* Stage 9: pipx outside of bisos testing

Goal: Make sure that startAiActivity works fine outside of bisos


* Stage 9 TODOs
** +
** DONE [#A] Setup an Examples repo for the templates and test against it. :me:
CLOSED: [2026-07-15 Wed 15:12] SCHEDULED: <2026-06-09 Tue>
----- AI Results: ------
/bxRepos/bxexamples/aiActivityTemplates
** DONE [#A] After upload to pypi, test it as a pipx install in a fresh Debian Machine :me:
CLOSED: [2026-07-15 Wed 15:14] SCHEDULED: <2026-06-09 Tue>
----- AI Results: ------
Verified.
** DONE [#A] Update Documentation to include Examples Repo specifically. :aiAndMe:
CLOSED: [2026-07-15 Wed 15:14] SCHEDULED: <2026-06-09 Tue>
----- AI Results: ------
Verified.
** -


* Stage 10: Post-launch refinements --- provenance, --noLink, custom activity

Goal: Address rough edges that emerged when using =bisos.startAiActivity=
on a real hierarchical project (site-status-scripts) --- three related
changes bundled together because they came out of the same field-usage
session:

1. Provenance recording in safe-copied files (the =templates=<X>= mistake
   would have been caught faster with visible birth-certificate data)
2. =--noLink= CLI override for symlinked files (needed by =custom= activity
   where =AI-Activity.org= must be per-project, not shared)
3. New =custom/= activity in all three templates trees (BISOS, bxexamples,
   T-Mobile fork), for projects whose conventions don't fit any standard
   activity


* Stage 10 TODOs
** +
** DONE [#A] <<Provenance Line>> Multi-line org-comment provenance in safe-copied files :ai:
CLOSED: [2026-07-16 Tue] SCHEDULED: <2026-07-16 Tue>
After the safe-copy of =AI-DevStatus.org= and =AI-WorkPlan.org= (and
before dblock expansion), insert a multi-line org-comment block
recording: date (ISO 8601 with tz), user (=USER= env → =LOGNAME= env
→ =os.getlogin()= → =pwd.getpwuid(os.getuid())=), templates base
(actually used, via =_resolveTemplatesBase=), and =sys.argv=. Placement:
after the last =#+= directive at the top of the file. Not a dblock
--- inert text, written once at install time, never regenerated.
Rationale: elisp dblock expanders can't recover invocation context;
provenance must be birth-certificate data.
----- AI Results: ------
Implemented in =py3/bin/startAiActivity.cs=. New helpers
=_detectUser()= (4-tier fallback chain, handles nohup/detached shells
where =os.getlogin()= raises) and =_writeProvenanceLine()= (scans for
last =#+= header, inserts multi-line comment block). Called from
both =initiate= and =initiateSub= safe-copy sites. Verified in test
runs --- provenance appears at the top of both editable files with
correct date, user (=bystar=), templates path, and full command line.
=aiSuspend= / =aiResume= treat provenance as opaque file content, so
it survives suspend/resume cycles unchanged.
** DONE [#A] <<noLink Override>> --noLink CLI flag for safe-copy override :ai:
CLOSED: [2026-07-16 Tue] SCHEDULED: <2026-07-16 Tue>
Add =--noLink=<filename>= parameter to both =initiate= and =initiateSub=.
When passed, the named file is safe-copied instead of being installed
as a symlink. Applies to =CLAUDE.md=, =AI-WORKFLOW.org=, and
=AI-Activity.org= (files the engine would otherwise install as
symlinks). Not applicable to =.claude/= entries. Single filename only
for now; multi-file support later via a =bisos.b= framework improvement
(=nargs='+'= or =append= action on =parDictAdd=).
----- AI Results: ------
Implemented. Chose CLI-flag over per-activity config file after
discussion --- preserves the principle that templates should stay
passive (a filesystem of examples, not a configuration layer). Two
new =parDictAdd= entries (=noLink= in the =commonParamsSpecify=
common list), classHead dblocks for both =initiate= and =initiateSub=
extended with =noLink= in =parsOpt=, and the symlink-installation
sites in both commands now branch on =noLink == fname= to select
=shutil.copy2()= vs =symlink_to()=. Log messages distinguish the
two behaviors (=SYMLINKED:= vs =COPIED (--noLink=<file>):=). All four
combinations tested (default, =--noLink=AI-Activity.org=,
=--noLink=CLAUDE.md=, =initiateSub --noLink=AI-Activity.org=).
=verify.sh= regression-clean.
** DONE [#A] <<custom Activity>> Add custom/ activity to all three templates trees :ai:
CLOSED: [2026-07-16 Tue] SCHEDULED: <2026-07-16 Tue>
The =custom= activity is the escape hatch for projects whose
conventions don't fit any standard activity. Its =AI-Activity.org=
is meant to be safe-copied via =--noLink=AI-Activity.org= and owned
by each project. Add to T-Mobile fork first, then copy structure
to BISOS and bxexamples so all three templates trees stay aligned.
Also update =examples= menu output to advertise the =--noLink=
form after each =--activity=custom= line (three menu chapters ×
one custom line each = three additions).
----- AI Results: ------
=custom/AI-Activity.org= authored with proper per-project template
framing (explains =--noLink=AI-Activity.org=, provides placeholder
sections for project conventions). Copied to all three template
trees; three-way md5 match confirmed. =examples= menu updated: each
=--activity=custom= line now followed by a =--noLink=AI-Activity.org=
variant with an explanatory comment.
** DONE [#A] <<Stage 10 Docs>> Update README + DevStatus + WorkPlan + templates READMEs :ai:
CLOSED: [2026-07-16 Tue] SCHEDULED: <2026-07-16 Tue>
Four documentation edits: engine =README.org= new subsection under
Usage covering =--noLink=; engine =AI-DevStatus.org= updates to
Current State + Key Design Decisions; engine =AI-WorkPlan.org= this
Stage 10 entry; per-activity Note for =custom= in each of the three
template trees' READMEs.
----- AI Results: ------
Documentation updates in progress at time of writing this AI Result;
the four edits are being applied in the same session that closed the
three prior Stage 10 TODOs.
** -


* Stage 11: CLAUDE.md safe-copy + refresh command

Goal: Fix a resolution bug discovered in a fresh Claude session against
=site-status-scripts=. =CLAUDE.md= was symlinked into the templates
directory. Claude Code resolves symlinks before reading, so
=@./AI-Activity.org= inside the symlinked =CLAUDE.md= resolved against
=<templatesBase>/mother/=, pulling in the wrong =AI-Activity.org=
instead of the project's per-activity one. Silent misinformation ---
critical to fix.

Two related changes:

1. Make =CLAUDE.md= a safe-copy (like =AI-DevStatus.org= /
   =AI-WorkPlan.org=) so its =@./= imports resolve at the project.
   No provenance line --- =CLAUDE.md= must remain equivalent to a
   symlink in content.
2. Add a =refresh= command so template changes to =CLAUDE.md= can be
   pulled in on demand (safe-copy loses the automatic-propagation
   property of symlinks; =refresh= gives it back explicitly).


* Stage 11 TODOs
** +
** DONE [#A] <<CLAUDE.md Safe-Copy>> Switch CLAUDE.md from symlink to safe-copy in initiate/initiateSub :ai:
CLOSED: [2026-07-17 Wed] SCHEDULED: <2026-07-17 Wed>
=initiate= and =initiateSub= now =shutil.copy2= =CLAUDE.md= from
=mother/CLAUDE.md= (base) or =mother/initiateSub/CLAUDE.md= (sub)
instead of symlinking. No provenance line for =CLAUDE.md= --- must be
byte-identical to the template. =deClaudify= moved =CLAUDE.md= (and
=CLAUDE.md.dormant=) from the symlink-only list to the removable-files
list so it removes either form. =aiSuspend= now =rename=-s =CLAUDE.md=
→ =CLAUDE.md.dormant= (file, not symlink); if a legacy symlink is
found, it is unlinked. =aiResume= promotes =CLAUDE.md.dormant= back
if present, or re-copies from templates.
----- AI Results: ------
Mode detection needed to change: previously =aiResume= and =initiateSub=
compared the =CLAUDE.md= symlink target against
=<templatesBase>/mother/initiateSub/CLAUDE.md= to distinguish sub from
base. Since =CLAUDE.md= is now a copy, both switched to walking up for
a parent =AI-WORKFLOW.org= symlink under =templatesBase=. =AI-WORKFLOW.org=
remains a symlink (harmless --- it has no =@./= back-imports), so this
signature is still available. =initiateSub= precondition and =aiResume=
mode-detection both use the same walk-up helper logic (inlined for now;
extract to a helper if a third caller emerges).
** DONE [#A] <<refresh Cmnd>> New refresh command to re-copy CLAUDE.md from templates :ai:
CLOSED: [2026-07-17 Wed] SCHEDULED: <2026-07-17 Wed>
New =refresh= Cmnd (=parsOpt="templates"=): detects base vs sub via
parent-AI-WORKFLOW.org walk-up under =templatesBase=, then re-copies
=CLAUDE.md= from =mother/= or =mother/initiateSub/=. Upgrades legacy
symlinked =CLAUDE.md= installs by unlinking then copying (this is the
one-shot migration path for pre-Stage-11 projects). Only touches
=CLAUDE.md= for now; per-project files (=AI-DevStatus.org=,
=AI-WorkPlan.org=, and any =--noLink= file) are never touched.
Multi-file =refresh= (parallel to multi-file =--noLink=) is deferred to
the same =bisos.b= framework improvement.
----- AI Results: ------
Cmnd class added after =aiResume= in =startAiActivity.cs=. Examples
menu got a new =refresh= chapter between aiSuspend/aiResume and
deClaudify. Log messages distinguish the three cases: =UPGRADED (legacy
symlink -> safe-copy)=, =REFRESHED=, =COPIED (was not present)=.
** DONE [#A] <<Stage 11 Docs>> README + AI-DevStatus + AI-WorkPlan updates :ai:
CLOSED: [2026-07-17 Wed] SCHEDULED: <2026-07-17 Wed>
Engine =README.org=: new subsection "Refresh CLAUDE.md From Templates"
under Usage (placed before Suspend/Resume) + TOC entry. Engine
=AI-DevStatus.org=: Current State entry describing the change and a
new Key Design Decision entry explaining why =CLAUDE.md= is
safe-copied (the =@./= symlink-resolution bug). Engine =AI-WorkPlan.org=:
this Stage 11 with three DONE TODOs.
----- AI Results: ------
Applied in the same session that implemented the code changes.
** -

* Stage 12: Maintenance and New Feature Ideas

Goal: Based on usage consider new features, and non-urgent bugs.

* Stage 12 TODOs
** +
** TODO [#A] Add a .startAiActicity.cs to the currect directory that captures and defines templates directory :aiAndMe:
SCHEDULED: <2026-07-21 Tue 13:41>
In stuations when one works with different template dirs it is possible to confuse recrecreations and resumes.
This makes it convenient. Also upon creation of the repo this can be planted to move towrds less config.
----- AI Results: ------
** TODO [#A]  In ~/.config/bisos/startAiActivity.cs  create and fps directory and  move the fps there
SCHEDULED: <2026-07-21 Tue 13:41>
This will then become a stardard for persistent user parameters. Also consider brining current here.
----- AI Results: ------

** -


# Local Variables:
# eval: (setq-local toc-org-max-depth 4)
# eval: (setq-local org-log-done 'time)
# End:
